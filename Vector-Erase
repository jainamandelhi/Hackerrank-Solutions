#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#define ff for(int i=0;i<arr.size();i++)
using namespace std;


int main() {
    int n;
    cin>>n;
    vector< int>arr(n);
    ff
        cin>>arr[i];
    int x,a,b;
    cin>>x;
    arr.erase(arr.begin()+x-1);
    cin>>a>>b;
    arr.erase(arr.begin()+a-1,arr.begin()+b-1);
    cout<<arr.size()<<endl;
    ff
        cout<<arr[i]<<" ";
    return 0;
}
